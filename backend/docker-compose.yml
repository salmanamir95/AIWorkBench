version: '3.8'

services:
  ai:
    build: ./ai
    ports:
      - "8081:8080"

  auth:
    build: ./auth
    ports:
      - "8082:8080"

  gateway:
    build: ./gateway
    ports:
      - "8083:8080"

  project:
    build: ./project
    ports:
      - "8084:8080"

  security:
    build: ./security
    ports:
      - "8085:8080"

  task:
    build: ./task
    ports:
      - "8086:8080"

  user:
    build: ./user
    ports:
      - "8087:8080"
